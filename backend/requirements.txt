# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# CORS support
fastapi-cors==0.0.6

# Data models and validation
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
email-validator==2.1.0

# Document processing (Word documents)
python-docx==1.1.0

# Database support
sqlalchemy==2.0.23
alembic==1.12.1

# Authentication and Security
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# Async file support
aiofiles==23.2.1

# Google Gemini AI (lightweight alternative to heavy ML libraries)
google-generativeai==0.8.3

# Optional: Caching (gracefully degrades if not available)
redis==5.0.1

# Optional: Vector database (gracefully degrades if not available)
qdrant-client==1.7.0

# RAG Dependencies (for semantic search)
sentence-transformers==2.2.2
torch==2.0.1
transformers==4.35.0
